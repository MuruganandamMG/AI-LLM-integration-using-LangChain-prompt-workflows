# Contributing Guidelines

1. Fork and create a feature branch (`feature/your-feature-name`).
2. Run pytest suite (`pytest -v`) to ensure 100% test coverage.
3. Commit with semantic conventions (`feat:`, `fix:`, `docs:`, `test:`).
4. Submit a Pull Request for review.
